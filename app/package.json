{
  "name": "game-life-arcade",
  "version": "1.0.0",
  "private": true,
  "description": "Native shell for the Game Life Arcade (lifegameproject.com) — bundles the whole static site offline-first via Capacitor.",
  "scripts": {
    "build:www": "node scripts/sync-www.js",
    "ios": "npm run build:www && npx cap sync ios && npx cap open ios",
    "sync": "npm run build:www && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
