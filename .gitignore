# Secrets — never commit
SECRET.md

# Large external/cloned repo that landed in this folder
houfu0702.github.io/

# Source/reference media used to trace scenes (kept local, not deployed)
canadaflag.png
cntower.png
doublepul.png
worldmap.png

# Large source videos (the demo/gif/*.gif are what the site/README use)
demo/*.MOV
demo/*.mov

# Raw screen-recording sources for the feature GIFs (kept local, not deployed)
finaldemo/

# OS / editor junk
.DS_Store
*.swp

# itch.io build artifact
lifegame-itch.zip

# Raw source recording for the hero gif (kept local)
demojun25/

# Tomato dFBA source/reference (engine, models, handoff) — kept local, not deployed
game/tomato/

# Personal interview notes — kept local
INTERVIEW_PREP.md

# Glyph Run paleography reference books (copyrighted scans; 字源 alone is 137MB
# > GitHub's 100MB limit) — kept local, never deployed. Index: refs/README.md
refs/*.pdf

# CC-BY guzheng source WAVs (huge) — only the encoded game/media/*.mp3 ships
guzheng/
