# Secrets — never commit
SECRET.md

# Large external/cloned repo that landed in this folder
houfu0702.github.io/

# Source/reference media used to trace scenes (kept local, not deployed)
canadaflag.png
cntower.png
doublepul.png
worldmap.png

# Large source videos (the demo/gif/*.gif are what the site/README use)
demo/*.MOV
demo/*.mov

# OS / editor junk
.DS_Store
*.swp
